server {
    listen 80;
    listen [::]:80;
    server_name ksteps.yinbo.online;
    location /.well-known/acme-challenge/ { root /var/www/letsencrypt; }
    location / { return 503 "KnowledgeSteps is being deployed. Please try again shortly.\n"; }
}
