# 前端接口待办

更新日期：2026-09-12。真实接口主体已接入，当前重点是异常分支和交付验收。调用规则见 [接口对齐](FRONTEND_API_ALIGNMENT.md)，跨模块待办见 [undo.md](../../undo.md)。

## 已完成

- [x] 六个寻路接口及管理员登录、当前用户和退出接口接入。
- [x] Cookie、CSRF、运行时响应校验与统一错误处理；写请求不自动重放。
- [x] API 与 Mock 模式隔离，生产默认 API，Mock 仍需认证。
- [x] 15 秒请求超时覆盖响应体读取，支持取消与计时器清理。
- [x] Retry-After 解析；尚未统一接入页面等待策略。
- [x] 用户任务归属、Mock 多标签页缓存和退出期间写入保护。
- [x] 本地自评、清空全部选择、顺序目录及提交时逐题保存。
- [x] 提交同步锁、页面离开后停止后续提交和忽略完成响应。
- [x] 完整图谱、四档资料上限、节点资料状态与零节点提示。
- [x] 任务切换重建页面状态，避免旧题目、结果和抽屉残留。
- [x] 前端自动回归与加载控制器测试。

## 待实现或确认

- [ ] 为资料 PENDING 增加独立提示，避免被作为普通零资料结果展示。
- [ ] 对资料读取 404、409 等错误提供更准确的恢复操作。
- [ ] 给会话 warnings 增加非阻断汇总入口。
- [ ] 评估轮询的有限退避和手动恢复，替代非终止错误下无限两秒重试。
- [ ] 将 Retry-After 用于页面限流提示与等待策略，保留写请求不自动重放。
- [ ] 明确生产域名、同源 API 代理和 SPA 回退，在实际部署环境验证子路由刷新。
- [ ] 评估收紧 package.json 中 latest 范围；当前已有 lockfile，应使用 npm ci，不应描述为完全没有依赖锁定。

- [ ] 将现有 test:regressions 与 test:loading 纳入 CI；当前前端 CI 仅运行 lint、typecheck、build。

## 待验收

- [ ] 真实模型、知乎检索完整链路，记录版本、环境和脱敏结果，不在文档粘贴密钥。
- [ ] 创建、生成失败、零前置节点、自评、清空、复核、提交、搜索、结果及资料阅读。
- [ ] 逐题提交失败、连续点击提交、离页和切换任务，确认无旧响应回跳。
- [ ] 两账号越权、退出后访问旧任务、Cookie/CSRF 失效及生产 HTTPS。
- [ ] 非常了解仍保留节点，实际资料数量不超过各档上限，目标没有推荐资料。
- [ ] 桌面和窄屏、键盘、减少动态效果；同层六个节点按五加一换行，连线避让。
- [ ] 刷新问卷恢复服务端答案；本地未提交选择不承诺恢复。
- [ ] 刷新结果路由与登录返回路径，确认部署无 SPA 404。

## 不纳入当前实现

知乎 OAuth 尚未接通；用户中心、结果导出、分享、资料重新搜索、批量原子答卷和草稿持久化需另定范围。不得为文档同步新增这些功能。

## 最近验证

2026-09-12：前端 lint、typecheck、build，通过 14 项回归测试及 6 项加载测试；后端 99 项测试通过。此记录不等同于真实服务或浏览器验收完成。

历史寻路已接通：顶部入口跳转 `/history`，显示当前用户总数与分页寻路卡片（目标名及日期）；READY 进入问卷，其他状态进入结果页。支持空态、加载、刷新重试及 Mock 按用户隔离。
