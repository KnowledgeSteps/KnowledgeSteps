# 待办与验收记录

更新日期：2026-09-12。依据当前代码及本轮审查整理；已完成表示代码已实现，不代表所有浏览器场景均已验收。

## 待验收

- [ ] **任务切换与提交中断**

  涉及：[问卷页面](frontend/src/pages/SessionQuestionsPage.tsx)、[提交逻辑](frontend/src/components/quiz/submitAssessment.ts)、[结果页面](frontend/src/pages/SessionResultPage.tsx)。

  验收：模拟慢请求，在提交期间离开页面或切换任务，确认不继续提交剩余答案、不跳回旧任务，不残留旧问卷、结果和资料抽屉。连续点击“查看结果”只能启动一次提交；保存失败后保留本地答案并允许重试。提交中断逻辑已有自动测试，页面交互仍需浏览器验证。

- [ ] **提交前清空全部选择**

  当前选项先保存在前端，点击“查看结果”后才逐题保存到后端。旧版“每选一题立即保存”的验收描述已废弃。

  验收：部分作答、全部作答和复核时，清空按钮均清除全部本地选择，题号回到第一题，进度与目录同步复位；清空操作不发送后端写请求。无选择或提交期间按钮禁用。

- [ ] **桌面与窄屏视觉、键盘交互**

  验收：问卷桌面侧栏与左侧等高且列表可滚动；窄屏内容无异常裁切。图谱每行最多五个节点，第六个换行，横向滚动可访问全部节点；不同卡片高度、换行及跨层依赖下，淡蓝色直角折线连接正确并避让卡片，无箭头、圆点及从下向上的卡片入场动画。检查键盘焦点、加载、禁用和失败状态。

- [ ] **真实 AI 与资料接口联调**

  验收：正常生成、模型超时、JSON 无法修复、结构校验失败和零前置节点分别展示正确提示；资料推荐数量遵守 0、2、3、5 上限。网络读取失败可以重新读取；已保存的搜索失败不出现误导性的重新搜索入口。

## 文档同步

- [x] **统一当前实现与旧说明（2026-09-12）**

  已同步 README、项目说明、接口与数据库文档、迁移部署说明、AI 校验说明、前端实现/接口对齐/待办/验收计划、视觉规范及 AGENTS。明确逻辑层级与每行五个的视觉换行区别，更新本地作答与提交保存、完整图谱、节点卡片及静态直角折线规则；保留主页 Transformer 示例的独立动画。PR 模板及校验规则已核对，格式保持不变。

  尚未完成的 PENDING 状态、warnings 汇总、轮询退避、Retry-After 页面使用及前端测试接入 CI 等事项见 [接口待办](frontend/docs/FRONTEND_API_TODO.md)，未标记为实现完成。

## 已知边界与可选增强

以下不是已承诺的修复任务，实施前需明确产品范围。

- **图谱层数由提示词引导**：后端校验 DAG、节点有效性和目标可达性，尚未硬性限制金字塔层数和层宽。前端五个换行不改变逻辑层级；不得为凑外观而伪造知识依赖。
- **答案逐题保存**：提交中断前已保存的答案不会回滚；本地未提交修改刷新后不保留。如需整体原子提交或草稿恢复，需另行设计。
- **资料搜索失败不支持重新搜索**：当前仅支持接口读取重试。若增加重新搜索，需要权限检查、限流、状态更新和重复请求控制。

## 已完成

- [x] **身份与 Mock 数据隔离**：所有任务操作检查用户归属；异步模拟写入前复核登录态，退出后拒绝写入；无归属旧缓存不自动迁入。
- [x] **Mock 多标签页兼容**：随机唯一任务 ID、每任务独立 v3 缓存，读取和修改前刷新缓存；兼容有归属 v2 数据。
- [x] **请求超时与取消**：前端请求超时覆盖响应体读取，完成后清理计时器；不自动重放写请求。生产构建默认真实 API。
- [x] **页面生命周期保护**：首页创建任务增加离页保护；问卷与结果按 sessionId 重建状态，旧提交完成后不更新或跳转；同步锁防止重复提交。
- [x] **自评交互**：本地作答、提交前清空全部选择、顺序目录、当前题与已答标记；提交期间禁用答题和题目导航。
- [x] **资料错误提示**：区分网络读取失败与已保存的搜索失败，删除无效的重新搜索入口。
- [x] **AI JSON 校验**：图谱与问卷分别校验，有限修复格式并补齐可选空值，拒绝缺失关键内容或无效结构；生成失败错误分类展示。
- [x] **资料推荐策略**：保留完整图谱，按自评熟悉程度提供最多 0、2、3、5 条资料，目标节点不搜索资料。
- [x] **界面更新**：统一 Ant Design、品牌主题及字体；更新等待进度、简约失败弹窗、零节点提示、自评区和结果节点卡片。
- [x] **图谱展示**：每行最多五个节点，超出换行；卡片持续显示，连线采用淡蓝色直角折线并避让卡片，移除曲线旧代码。
- [x] **主页示例交互**：Transformer 示例分别管理悬停、键盘聚焦与点击固定，支持再次点击或 Escape 收起。

## 最近验证结果

本轮代码审查后已执行：

- 前端 lint、typecheck、build：通过。
- 前端回归测试：14 项通过，覆盖隔离、缓存、请求超时、图谱避让和提交中断等逻辑。
- 加载状态测试：6 项通过。
- 后端 Maven 测试：99 项通过，无失败或跳过。
- 浏览器视觉与真实服务联调：未在本轮完成，见待验收清单。

本次同步文档并修正图谱提示词的层/行说明；文档相对链接和格式检查通过。本次提示词相关回归检查 24 项通过，不据此宣称视觉或模型质量验收通过。
