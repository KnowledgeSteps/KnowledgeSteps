# 待处理问题

记录日期：2026-09-11。以下问题来自代码阅读；状态随修复更新。

- [x] 保存答案期间禁止返回上一题

  位置：[QuestionPanel.tsx](frontend/src/components/quiz/QuestionPanel.tsx) 第 56 行、[SessionQuestionsPage.tsx](frontend/src/pages/SessionQuestionsPage.tsx) 第 69 行。

  原触发条件：答案请求尚未完成时，用户点击“返回上一题”。本次视觉重构已为返回按钮补上 saving 禁用条件；慢请求与保存失败场景仍待专项验收。

  影响：旧请求完成后会按此前计算的下一题再次跳转，打断用户操作；返回后的题目选项看似可点击，但保存状态检查会忽略点击。

  建议：保存期间同时禁用返回按钮与题目导航，确保保存状态和可操作状态一致。

  验收：模拟慢请求，确认保存期间无法跳题；保存失败后恢复操作，并保留当前题目和错误提示。

- [x] 修正资料搜索失败后的重试行为

  位置：[ResourcesDrawer.tsx](frontend/src/components/result/ResourcesDrawer.tsx) 第 111 行、[JdbcSessionStore.java](backend/src/main/java/com/zhihu/hackathon/session/JdbcSessionStore.java) 第 218 行。

  触发条件：节点的资料状态为 FAILED，用户点击“再试一次”。

  影响：按钮仅重新调用资料 GET 接口，读取数据库中已保存的失败状态，不会重新搜索知乎，因此无法通过该操作恢复资料。

  建议：区分“接口读取失败”和“上游搜索失败”。前者保留读取重试；后者先取消无效重试入口并说明情况。如果需要重新搜索，再单独实现带权限检查、限流和状态更新的重试能力。

  验收：读取失败可重新加载；已保存的搜索失败不会显示能够重新搜索的误导性入口。

- [x] 统一前端视觉实现与产品约定

  已统一为 Ant Design、Ant Design Icons、蓝白主题、Chillax 与思源黑体，并更新 UI_DESIGN.md 和 AGENTS.md。公共主题由 frontend/src/design/theme.ts 管理。

## 2026-09-11 审查修复

- [x] Mock 按 userId 分开保存与读取，所有任务操作检查归属；旧版无归属缓存不自动迁入。异步模拟请求执行前复核登录态，退出后不再写入。
- [x] 生产构建未指定数据模式时默认使用真实 API；开发环境仍可使用 Mock。
- [x] 已保存的资料搜索 FAILED 移除无效重试；网络读取失败保留“再试一次”。尚未实现重新搜索能力，界面明确说明。
- [x] 问卷及结果页面按 sessionId 重新挂载，切换任务不保留上一份题目、结果和侧栏状态。
- [x] 问卷保存和完成请求回调检查页面存活状态，离开后不再更新或跳转。
- [x] HTTP 请求增加 15 秒超时和取消机制，覆盖响应体读取，完成后清理计时器；超时不会自动重放写请求。

验证：用户隔离、缓存归属、退出期间的模拟写请求、超时、响应体挂起、取消与计时器清理有自动回归测试。任务切换、慢请求期间离开页面、按钮禁用仍需浏览器专项验收。

## 再次审查修复

- [x] Mock 使用随机唯一 ID 与每任务独立 v3 缓存，读取/修改前重新读取缓存；保留有归属 v2 数据的兼容读取。双标签页创建、交叉读取及顺序修改测试通过。
- [x] 首页创建任务加入离页保护，离开后请求不再导航或更新页面状态。
- [x] Transformer 分开管理悬停、键盘聚焦与点击固定状态，第一次点击固定展开，第二次点击或 Escape 收起。
